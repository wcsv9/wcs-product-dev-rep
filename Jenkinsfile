pipeline {
    agent none
        stages {
        stage('checking out') {
            agent { label 'wcsv9' }
            steps { 
             
              dir ('/opt/Deploy') {
               
             echo "Done"
              }
              
            }
        }}
}
