pipeline {
    agent none
    
        stages {
	wrap([$class: 'Xvfb', screen: '1440x900x24']) 
  // execute selenium tests

        stage('checking out') {
            agent { label 'wcsv9' }
            steps { 
             echo "Checking out to Mounted Volume"
              
            }
        }
		stage('Running Test and Checking Title') {
            agent { label 'wcsv9' }
            steps { 
             sh "cd /data/wcscode && mvn  install"
              
            }
        }
            
            }
            }
