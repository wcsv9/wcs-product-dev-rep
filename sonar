
 node ('master') {
   stage('SonarQube analysis') {
   ws('/var/lib/jenkins/workspace/sonar/sonar-scanner.properties')
    // requires SonarQube Scanner 2.8+
    def scannerHome = tool 'sonar';
    withSonarQubeEnv('sonar') {
      sh "${scannerHome}/bin/sonar-scanner"
    }
  }
}
